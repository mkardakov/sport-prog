{% extends 'base.html.twig' %}

{% block title %}Muscle{% endblock %}

{% block body %}
    <h1>Muscle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ muscle.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ muscle.title }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ muscle.location }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('muscle_index') }}">back to list</a>

    <a href="{{ path('muscle_edit', {'id': muscle.id}) }}">edit</a>

    {{ include('muscle/_delete_form.html.twig') }}
{% endblock %}
